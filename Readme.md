# 🎬 Movie Data Pipeline (Data Engineering Assignment)

This project builds a simple **ETL pipeline** that extracts movie and rating data from CSV files, enriches it using the **OMDb API**, and loads it into an **SQLite** database for analysis.

---

## 🚀 Project Structure
movie-data-pipeline/
├── etl.py
├── schema.sql
├── queries.sql
├── requirements.txt
└── data/
├── movies.csv
└── ratings.csv